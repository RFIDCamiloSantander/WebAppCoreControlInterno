@using WebAppCoreControlInterno.Models.ViewModels
@model EstadoViewModel

@{ViewBag.Title = "Crear Estado";}

@await Html.PartialAsync("_NavbarAdministracion")

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-12 col-md-4">
            <div class="card">
                <div class="card-body">
                    <h1 class="card-title">Crear Estado</h1>
                    <form asp-action="CrearEstado">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <p class="text-danger text-center h6">@ViewBag?.ErrorMessage</p>
                        <div class="mb-3">
                            <label class="form-label" for="txtCargo" asp-for="Estado1">Estado</label>
                            <input class="form-control" type="text" id="txtCargo" asp-for="Estado1" />
                            <span class="text-danger" asp-validation-for="Estado1"></span>
                        </div>
                        <div class="row">
                            <div class="col">
                                <a class="btn btn-info" asp-action="IndexCargo">Volver</a>
                            </div>
                            <div class="col text-end">
                                <button class="btn btn-primary" type="submit">Crear Estado</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
